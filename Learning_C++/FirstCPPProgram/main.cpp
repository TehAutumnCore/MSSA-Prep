#include <iostream> //standard input/output library

int main() { //entry point of the program
    std::cout << "Hello, C++!" << std::endl; //std is the namespace, cout is the standard output stream object in the std namespace, << is the stream insertion operator, and endl is like \n or writeline()
    return 0; 
}